# --- Application configuration (read by the server) ---
HOST=0.0.0.0
PORT=50051
METRICS_PORT=9100
MGMT_PORT=9000

# gRPC Options
GRPC_REFLECTION=true
GRPC_LOGGING=true

# --- Docker Compose host-port mapping (observability stack) ---
# GRPC_PORT maps the host port for the mock in docker-compose.observability.yaml.
# Keep it in sync with PORT above.
GRPC_PORT=50051
GRAFANA_PORT=3000
PROMETHEUS_PORT=9090
